<h1><%= @blog.title %></h1>

<p><%= @blog.content %></p>

<p>
  <%= link_to "ブログ一覧へ", blogs_path %>
  <%= link_to "編集する", edit_blog_path(@blog.id) %>
  <%= link_to "削除する", blog_path(@blog.id), method: :delete, data: {confirm: "タイトル「#{@blog.title}」の記事を削除してよろしいですか？"}  %>
</p>
