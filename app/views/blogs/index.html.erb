<h1>ブログ一覧</h1>

<table>
  <% @blogs.each do |blog|%>
    <tr>
      <td><%= blog.title %></td>
      <td><%= blog.content %></td>
      <td><%= link_to "見る", blog_path(blog.id) %></td>
      <td><%= link_to "編集", edit_blog_path(blog.id) %></td>
      <td><%= link_to "削除", blog_path(blog.id), method: :delete, data: {confirm: "タイトル「#{blog.title}」の記事を削除してよろしいですか？"} %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "ブログ作成", new_blog_path %></p>

